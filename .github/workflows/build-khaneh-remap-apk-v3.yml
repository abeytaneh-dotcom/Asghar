name: Update APK build

on:
  push:
    branches:
      - apk-build-v3-20260925
    paths:
      - .github/workflows/build-khaneh-remap-apk-v3.yml
      - build-update.sh
      - patch-update.py

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo BUILD_WORKFLOW_OK
